'use strict';

// Model constants — no project imports to avoid circular dependencies.
const MODEL = 'claude-sonnet-4-6';
const SYNTHESIZER_MODEL = 'claude-haiku-4-5-20251001';

module.exports = { MODEL, SYNTHESIZER_MODEL };
